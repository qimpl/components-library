type TitleProps = {
  variant: TitleVariant;
  children: React.ReactNode;
  color?: string;
};

type StyledTitleProps = {
  fontSize: string;
  titleColor: string;
};

export { TitleProps, StyledTitleProps };
